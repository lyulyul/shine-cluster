[Unit]
Description=Kill overtime processes when free memory is low

[Service]
User=root
ExecCondition=/usr/bin/bash -c "[[ $(free -g | awk 'NR==2 {print $3}') -ge 10 ]]"
ExecStart=/usr/local/sbin/killOvertime
